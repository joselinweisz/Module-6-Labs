#Joselin Weisz
#2/17/25
#Problem 6: Use a for statement to calculate the factorial of a user input value.
#Print this value as wel as the calculated value using the factorial function in the math module.



import math

number = input("Enter a number: ")
number = int(number)


factorial = 1
for i in range(1, number + 1):
    factorial *= i

math_factorial = math.factorial(number)

print(f"Factorial of {number} using for loop: {factorial}")
print(f"Factorial of {number} using math.factorial: {math_factorial}")
